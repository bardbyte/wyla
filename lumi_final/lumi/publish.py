"""Stage 4: Learn from run + publish to GitHub.

Built in Session 3-4. All deterministic — no LLM calls.

Key functions:
  update_learnings(coverage_report, learnings_path) -> None
  merge_and_write_output(enriched_outputs, baseline_dir, output_dir) -> None
  publish_to_github(output_dir, config) -> str  # returns PR URL
"""

# TODO: Session 3/4 — implement here
