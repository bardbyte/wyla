# secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
# GCP service-account keys (the script refuses to load them from inside the repo,
# but belt-and-suspenders so they can never accidentally land in git either).
*-service-account*.json
*service-account-key*.json
**/prj-*.json
gcp-key*.json
service_account*.json

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
*.egg
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# IDE
.idea/
.vscode/
*.swp
*.swo

# project-specific
.mdm_cache/
output/
*.log
.claude/settings.local.json
